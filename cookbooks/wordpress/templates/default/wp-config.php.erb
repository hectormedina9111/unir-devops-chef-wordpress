<?php
/**
 * Configuración de WordPress generada por Chef
 */

define( 'DB_NAME',     '<%= @db_name %>'     );
define( 'DB_USER',     '<%= @db_user %>'     );
define( 'DB_PASSWORD', '<%= @db_password %>' );
define( 'DB_HOST',     '<%= @db_host %>'     );
define( 'DB_CHARSET',  'utf8'                );
define( 'DB_COLLATE',  ''                    );

define( 'AUTH_KEY',         '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );
define( 'SECURE_AUTH_KEY',  '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );
define( 'LOGGED_IN_KEY',    '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );
define( 'NONCE_KEY',        '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );
define( 'AUTH_SALT',        '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );
define( 'SECURE_AUTH_SALT', '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );
define( 'LOGGED_IN_SALT',   '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );
define( 'NONCE_SALT',       '<%= (0...64).map { (65 + rand(58)).chr }.join %>' );

$table_prefix = 'wp_';

define( 'WP_DEBUG', false );

define( 'WP_HOME',    '<%= @wp_url %>' );
define( 'WP_SITEURL', '<%= @wp_url %>' );

define( 'ABSPATH', '<%= @document_root %>/' );

if ( ! defined( 'ABSPATH' ) ) {
  define( 'ABSPATH', __DIR__ . '/' );
}

require_once ABSPATH . 'wp-settings.php';
